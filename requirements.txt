# Zep V3 SDK. Official LangGraph docs (2026) require zep-cloud >=3.23.0.
zep-cloud==3.28.0
langgraph==1.2.9

# Gemini LLM for the demo UI chat reply (gemini-2.5-flash-lite by default).
# Pin the tested pair to avoid expensive resolver backtracking in Docker.
google-genai==1.75.0
google-auth==2.56.2

# Local baseline stores
redis==6.4.0
qdrant-client==1.19.0
# Recent scikit-learn wheels require Python 3.13; the Docker image is Python
# 3.12 because grpcio/Qdrant currently publishes compatible Linux wheels there.
scikit-learn>=1.7,<2; python_version >= "3.13"
scikit-learn>=1.5,<2; python_version < "3.13"

# Lab utilities
python-dotenv==1.2.2
pydantic==2.13.4
rich==14.3.4
typer==0.27.1
pytest==8.3.4
requests==2.34.2
streamlit==1.60.0
# Streamlit 1.60 imports DEFAULT_EXCLUDED_CONTENT_TYPES, introduced after
# Starlette 0.41. Pin a compatible modern Starlette in shared/global Python
# environments where an older version might otherwise be retained.
starlette==1.3.1
